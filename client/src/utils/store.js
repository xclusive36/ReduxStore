import { createStore } from 'redux'; // import createStore from redux
import {reducer} from './reducers'; // import reducer from ./reducers

export default createStore(reducer); // export createStore(reducer)